const Users = () => (
  <section className="page-stack">
    <div className="page-heading">
      <div>
        <p className="eyebrow">Users</p>
        <h2>User administration</h2>
      </div>
    </div>
    <div className="panel muted-panel">User management can be connected when the backend exposes admin user endpoints.</div>
  </section>
);

export default Users;
